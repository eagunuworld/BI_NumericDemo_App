apiVersion: v1
kind: Service
metadata:
  labels:
    app: lab-numeric
  name: numeric-svc
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
    nodePort: 30004
  selector:
    app: sel-numeric
  type: NodePort